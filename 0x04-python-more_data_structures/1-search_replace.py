#!/usr/bin/python3
def search_replace(my_list, search, replace):
    nlist = []
    for elem in mylist:
        if i != search:
            nlist.append(elem)
        else:
            nlist.append(replace)
    return nlist
